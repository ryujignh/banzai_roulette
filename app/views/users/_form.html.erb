<%= form_for Game.new do |f| %>

  <div>
    <%= f.label "Name" %><br>
    <%= f.text_field "name" %>
  </div>
  <div>
    <%= f.submit "Create a new game", :class => 'btn btn-success' %> 
  </div>
<% end %>